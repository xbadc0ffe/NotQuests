# FORK DIVERGENCE: upstream targets Paper 26.1.2; this fork targets 26.2, which
# the production server runs. Both require Java 25.
# Gradle pinned to match gradle/wrapper/gradle-wrapper.properties (9.5.1).

[tools]
java = "temurin-25"
gradle = "9.5.1"
